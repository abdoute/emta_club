# مثال لملف البيئة لمشروع EMTA Club
# انسخ هذا الملف إلى .env وغيّر القيم بما يناسبك للتطوير أو الإنتاج

# وضع Flask
# 0 = إنتاج (لا يُظهر تفاصيل الأخطاء)
# 1 = تطوير (يعرض Debugger وتفاصيل الأخطاء)
FLASK_DEBUG=0

# المنفذ الذي يعمل عليه الباك-إند محليًا
PORT=5000

# مفتاح سرّي لتطبيق Flask (استعمل قيمة قوية وعشوائية في الإنتاج)
SECRET_KEY=change-this-secret-key-in-production

# مفتاح سرّي لتوقيع JWT (يُفضّل أن يكون مختلفًا عن SECRET_KEY)
JWT_SECRET_KEY=change-this-jwt-secret-key-in-production

# مدة صلاحية التوكن بالثواني (هنا يوم واحد)
JWT_EXPIRES=86400

# عناوين الواجهة الأمامية المسموح لها بالوصول للـ API (CORS)
# في التطوير المحلي يمكنك مثلاً:
# CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500
# في الإنتاج استبدلها برابط موقعك الحقيقي:
# CORS_ORIGINS=https://emta-club.com,https://www.emta-club.com
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500

# حساب الأدمن الثابت الذي يتم إنشاؤه/تحديثه تلقائيًا عند تشغيل الباك-إند
ADMIN_FIXED_EMAIL=admin@example.com
ADMIN_FIXED_PASSWORD=change-this-admin-password

# قائمة إيميلات الأدمن المسموح لهم باستخدام لوحات الإدارة وواجهات /api/admin
# يمكنك وضع أكثر من إيميل مفصولين بفاصلة
# مثال: ADMIN_EMAILS=admin@example.com,another.admin@example.com
ADMIN_EMAILS=admin@example.com
